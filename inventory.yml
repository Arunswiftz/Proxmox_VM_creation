---
  all:
    hosts:
      proxmox1:
        ansible_host: 192.168.3.151
        ansible_port: 8006
        ansible_user: root       #need to change user
        ansible_ssh_pass: root   #need to change passwd
        ansible_connection: ssh
      proxmox2:
        ansible_host: 192.168.3.152
        ansible_port: 8006
        ansible_user: root       #need to change user
        ansible_ssh_pass: root   #need to change passwd
        ansible_connection: ssh
      proxmox3:
        ansible_host: 192.168.3.153
        ansible_port: 8006
        ansible_user: root       #need to change user
        ansible_ssh_pass: root   #need to change passwd
        ansible_connection: ssh